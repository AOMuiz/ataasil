import Head from "next/head";
import Link from "next/link";
import styled from "styled-components";
import Logo from "../src/components/Logo";
import { useRouter } from "next/router";

const courseCard = {
  ar: {
    title: "كتاب التوحيد للشيخ عثيمين",
    summary:
      "لكن لا بد أن أوضح لك أن كل هذه الأفكار المغلوطة حول استنكار  النشوة وتمجيد الألم نشأت بالفعل، وسأعرض لك الت",
  },
  en: {
    title: "The book of Tawhid of Sheikh Uthaymeen",
    summary: "lorem ipsum",
  },
};

export default function Home() {
  const { locale, locales, defaultLocale, asPath } = useRouter();
  console.log({ defaultLocale, locale, locales });
  const { title, summary } = courseCard[locale];

  return (
    <div className="p-5">
      <Head>
        <title>Ataasil University</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <div>
        <div
          style={{
            padding: "4px",
            marginRight: "4px",
          }}
        >
          <span>Current Language: </span>
          <span
            style={{
              borderRadius: "3px",
              backgroundColor: "blue",
              color: "white",
              padding: "2px",
            }}
          >
            {locale}
          </span>
        </div>
        <div>
          <p>Select Language</p>
          <SelectLanguage>
            <Link activeClassName={locale === "ar"} href={asPath} locale="ar">
              ar
            </Link>

            <Link activeClassName={locale === "en"} href={asPath} locale="en">
              en-US
            </Link>
          </SelectLanguage>
        </div>
      </div>

      <main>
        <Text className="font-bold underline text-5xl my-4">{title}</Text>
        <Text>{summary}</Text>
        <Logo />
      </main>
    </div>
  );
}

const Text = styled.p`
  color: var(--color-primary);
  line-height: 31px;
`;

const SelectLanguage = styled.div`
  display: flex;
  /* justify-content: space-between; */
  font-size: 1.5rem;
  gap: 1rem;
`;
